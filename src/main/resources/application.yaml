spring:
  application:
    name: chat

  datasource:
    url: jdbc:postgresql://ep-flat-wave-apsd8yjj-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require
    username: neondb_owner
    password: npg_crRxDu96mwfX
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:8080}